define([
        "knockout",
        "knockout.mapping",
        "sammy",
        "Scripts/models/pages/pagebase"
], function (ko, mapping, sammy, pagebase) {

    function pageBase(parent) {
        var self = new pagebase(parent);
        debugger;
        return self;
    }

    return pageBase;

});